let numeros = ""; 
      for (let i = 1; i <= 20; i++) {
        numeros += i + " "; // Concatena cada número com um espaço em branco
      }
      document.write(numeros); // Exibe os números concatenados